# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

# Official Packages

meteor-base             		              # Packages every Meteor app needs to have
mobile-experience       		              # Packages for a great mobile UX
mongo                   		              # The database Meteor supports right now
blaze-html-templates    		              # Compile .html files into Meteor Blaze views
session                 		              # Client-side reactive dictionary for your app
jquery                  		              # Helpful client-side library
tracker                 		              # Meteor's client-side reactive programming library
standard-minifiers      		              # JS/CSS minifiers run for production mode
es5-shim                		              # ECMAScript 5 compatibility for older browsers.
ecmascript              		              # Enable ECMAScript2015+ syntax in app code
accounts-base					                    # Base accounts support.
accounts-password                         # Support for using accounts with passwords.
check                                     # Support for check()ing arguments.
random                                    # Random number and string generation.
accounts-ui                               # UI-related functionality for accounts.

# Third-Party Packages

aldeed:simple-schema                      # Support for simple object schema validation.
artwells:accounts-guest			              # Support for automatically creating accounts for guests.
mizzao:user-status                        # Support for monitoring user status.
underscore                                # Explicitly require Underscore.
underscorestring:underscore.string        # Various string utilities.

# MeteorMUD Packages

meteormud:namespace                       # The MeteorMUD namespace and core extensible namespaces as needed.
meteormud:accounts                        # User account support for MeteorMUD.
meteormud:schemas                         # Schema support for validating and processing objects in MeteorMUD.
meteormud:templates                       # Template management and associated functionality for MeteorMUD.
meteormud:ui-cli                          # The command-line interface for MeteorMUD.
meteormud:parsers                         # Core parsing support for MeteorMUD.
meteormud:parsers-cli                     # Command-Line Interface parser.
meteormud:parsers-commands                # Parsers for general commands.
meteormud:parsers-expressions             # Parsers for expressions (commands, orders, etc).
meteormud:permissions                     # Access control for MeteorMUD.
meteormud:help                            # Core help support for MeteorMUD.
meteormud:commands-scaffolding            # Scaffolding commands for testing.
